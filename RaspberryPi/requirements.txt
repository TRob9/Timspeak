# Core Dependencies
pyyaml==6.0.1
numpy>=2.0.0
soundfile==0.12.1
sounddevice==0.4.6
requests==2.31.0

# LLM Provider Support
litellm>=1.40.0

# Speech-to-Text Engines (commented for faster install)
# Note: openai-whisper is large and slow on Pi - faster-whisper recommended
# faster-whisper==0.10.0
# openai-whisper==20231117  # Optional, slower
# google-cloud-speech==2.24.0
openai>=1.27.0

# Raspberry Pi GPIO
gpiozero==2.0.1
lgpio==0.2.2.0  # Backend for gpiozero

# USB HID (for keyboard emulation)
# Will be configured manually via gadget mode - no Python package needed

# Audio
pyaudio==0.2.14  # May require: sudo apt-get install portaudio19-dev

# Optional but recommended
python-dotenv==1.0.0
